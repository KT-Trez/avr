export enum NotificationSeverity {
  Error = 'error',
  Info = 'info',
  Success = 'success',
  Warning = 'warning',
}

export enum ProgressAction {
  Download = 0,
  Merge = 1,
}

export enum ProgressType {
  Audio = 0,
  AudioAndVideo = 1,
  Video = 2,
}
