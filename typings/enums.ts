export enum NotificationSeverity {
	Error = 'error',
	Info = 'info',
	Success = 'success',
	Warning = 'warning'
}

export enum ProgressAction {
	Download,
	Merge
}

export enum ProgressType {
	Audio,
	AudioAndVideo,
	Video
}